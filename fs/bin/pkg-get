#!/usr/bin/env python2

def _main(cmd, *packages):
	if cmd == "update":
		for line in open("/etc/pkg-sources").read().split("\n"):
			spawn("wget", args = [line, "/etc/pkg-index", "a"])
        yield

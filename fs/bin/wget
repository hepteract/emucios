#!/usr/bin/env python2

def _main(url, save = None, mode = "w"):
	remote = open("/dev/nic/http/" + url.replace(";", "\\;").replace(\
		"/", ";").replace("http://", ""))
	content = remote.read()
	printf(content.text)
	yield
	if save is not None:
		open(save, mode).write(content.text)
	yield
